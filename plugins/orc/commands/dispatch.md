---
description: Multi-agent dispatch — decompose a plan into dependency-ordered waves run in parallel by Cursor, Codex, or Claude
argument-hint: '[plan file path or freeform request]'
---

Invoke the `dispatch` skill (from the `orc` plugin) with the user's arguments: $ARGUMENTS

If no arguments are provided, prompt for either a plan file path or a freeform description of the work to decompose.
