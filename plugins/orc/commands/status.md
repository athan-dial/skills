---
description: Single-pane view of all orc system activity — active orchestration, autoresearch loops, backlog count, last commit
---

Invoke the `status` skill (from the `orc` plugin). Render the standard 3-column markdown table (system / status / detail). No arguments expected.
