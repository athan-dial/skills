---
description: Autonomous optimization loop — iteratively improve a measurable codebase metric (coverage, latency, accuracy, test pass rate)
argument-hint: '[metric or goal description]'
---

Invoke the `autoresearch` skill (from the `orc` plugin) with the user's arguments: $ARGUMENTS

If no arguments are provided, prompt for the metric to optimize and the success threshold.
