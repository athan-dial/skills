---
description: Verify a dispatched plan by running its Acceptance checks; on pass write pattern and trigger reverse bridge, on fail generate gap plan and trigger forward bridge
argument-hint: '[plan file path or plan slug (optional)]'
---

Invoke the `verify` skill (from the `orc` plugin) with the user's arguments: $ARGUMENTS

