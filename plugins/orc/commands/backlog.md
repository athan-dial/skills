---
description: Lightweight backlog for the orc system — capture, list, triage, pick, or promote ideas
argument-hint: '[add <text> | list | pick <id> | triage | <freeform>]'
---

Invoke the `backlog` skill (from the `orc` plugin) with the user's arguments: $ARGUMENTS

If no arguments are provided, default to listing the backlog.
