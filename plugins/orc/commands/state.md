---
description: Surgical updates to .orc/state.json (set, add-job, note, touch, show)
argument-hint: '[show | set <k> <v> | add-job <agent:id:label> | clear-jobs | touch | note <text>]'
---

Invoke the `state` skill (from the `orc` plugin) with the user's arguments: $ARGUMENTS

If no arguments are provided, run `state show`.
