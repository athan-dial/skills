---
description: Session-start briefing from disk state — what happened last session, what's in the backlog, what to work on next
---

Invoke the `recap` skill (from the `orc` plugin). Designed for cold-start: this is typically the first message in a new session.
