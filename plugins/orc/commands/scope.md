---
description: Shape a backlog item or freeform idea into a scoped plan file before executing
argument-hint: '[<backlog-id> | <freeform description>]'
---

Invoke the `scope` skill (from the `orc` plugin) with the user's arguments: $ARGUMENTS

If no arguments are provided, ask the user whether to scope an existing backlog item (pick from list) or start from a freeform description.
