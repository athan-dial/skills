---
description: Grill-style interview that produces an orc-plan/1 directory ready for /orc:dispatch
argument-hint: '[goal in one sentence, or path to a markdown plan file]'
---

Invoke the `plan` skill (from the `orc` plugin) with the user's arguments: $ARGUMENTS

If no arguments are provided, prompt for a one-sentence goal or a markdown plan file path.
